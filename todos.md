# Todos

- [ ] Make markdown prettier. 
  - [ ] Center text, like in notion. not as in align-text:center. But as in a center wrapper or something. much nicer
  - [ ] More like how notion has it, where for example `test` is shown as a red text instead of with the ` ` . I think that is quite a lot nicer to read. 
  - [ ] Make tables format nicely. Like with the markdown plugin in nvim. So it gets the right width per cell and stuff, looks bad right now. (should get structured on save or something).